export interface EmployeeCheckIn {
  firstName: string;
  lastName: string;
  officeId: string
  programId: string;
  type: string;
  cellNumber: string;
  // consent: boolean;
  // noConsentReason: string;
}
